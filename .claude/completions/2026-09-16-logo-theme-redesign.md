# 2026-09-16 — New logo, light/dark theme, site polish

- Generated `image/logo-light.png` (black) and `image/logo-dark.png` (white) from `image/logo_black.png`, cropped and squared. Also added `favicon.png`, `favicon-dark.png`, and regenerated `logo.ico`.
- Moved the duplicated per-page CSS into `assets/css/style.css`. Light is the default, and dark applies through `prefers-color-scheme` or `html[data-theme]`.
- Added `assets/js/main.js` for the theme toggle (saved in localStorage), the mobile menu, and the footer year. An inline head script applies the saved theme before paint.
- All 8 pages now share the header (logo pair, nav, theme toggle, hamburger) and the footer logo. Fixed the footer `../` links on root pages.
- Homepage: new hero with the logo mark, two CTAs, a facts strip, a 3-card "Kimiz Biz", a checklist on Teknofest, and a closing CTA.
- Verified with headless Firefox screenshots (light/dark, desktop and 375px).
- Old `image/logo.png` and `image/logo_black.jpeg` are no longer referenced (left in place).

## Second pass: removing the "AI look"
- Installed Anthropic's official `frontend-design` skill at `~/.claude/skills/frontend-design` (source: github.com/anthropics/skills).
- Rewrote `assets/css/style.css`:
  - Palette: sea-ink `#0f2b3a` on white, with tether yellow `#f0b429` as the only accent. The dark theme uses deep navy.
  - Fonts: Archivo in its wide cut for headings and UI, Source Serif 4 for body text.
- Removed the card grids, uppercase eyebrow labels, "01 /" numbering, middle-dot meta strings and arrow suffixes.
- New layout:
  - Sections are rows, with the heading on the left and the content on the right.
  - Specs are shown as a table and blog posts as a list.
  - A dark sponsorship band closes the page.
  - The oversized, cropped nautilus spiral in the home intro is the one bold element.
- Pages are generated by a one-off script, so the root pages were rewritten entirely. Blog posts kept their article bodies.
- Moved the blog post data to `assets/js/posts.js`, shared by the home and blog pages.

## Third pass: calmer corporate palette, stable images
- Light theme:
  - Page background is a soft blue-grey `#f4f7f9`; the header and form fields are white.
  - Text is softened to `#1d2d38`.
  - The brand colour is navy `#1b4868`, used for primary buttons and the sponsorship band.
  - Gold `#c9a13b` is used only for thin marks (active nav, focus ring).
- Dark theme uses `#0f1a22` for the background and `#d5e0e7` for text.
- Toned down headline widths and weights (Archivo at 108%, weight 700).
- Home watermark: a large, faint (4.5%) nautilus inside the content column, always fully visible and no longer cropped by the viewport edge.
- Images now use fixed aspect ratios inside the content column, so the crop no longer changes with window size:
  - Home photo: 21:9 on desktop, 4:3 on mobile.
  - Sarıca render: 5:4, moved into the text column.

## Fourth pass: dark only (#141414), top info bar
- Removed the light theme, the theme toggle and the theme script. Pages now always use `logo-dark.png`.
- Added a thin top bar (`.topbar`) inside a sticky `.masthead` on all 8 pages:
  - Left: address and e-mail, each with an icon.
  - Right: "Bizi takip edin" followed by LinkedIn, Instagram and GitHub icons.
- `main.js` adds `.compact` to the masthead when `scrollY > 40`. That class slides the bar up by `--topbar-h` over a 0.35s transition, leaving only the nav; the bar comes back at the top of the page.
- Mobile: the address and the label are hidden; the e-mail and icons stay. The mobile nav dropdown is now positioned relative to the header.
- `image/logo-light.png` is unused now. The favicon still switches with the browser theme.

## Fifth pass: #0A0A0A, nav redesign, sponsorship page
- Colours: page `#0a0a0a`, top bar `#000`, header and surfaces `#121212`, rules `#222`.
- Header: bottom corners rounded to 10px, with a soft shadow.
- Nav order: Ana Sayfa, Araçlar, Blog, Galeri, Sponsorluk, İletişim.
  - Araçlar opens a dropdown on hover/focus, currently listing only Sarıca ROV. On mobile the item is shown indented.
  - Sponsorluk is a white button (`.nav-cta`); on its own page it gets a gold ring as the active state.
- Added `sponsorluk.html`, which reuses the existing sponsorship copy, the drive link and the contact details. Added Sponsorluk to the footer page list.
- Incident: a script truncated `index.html` because it opened the file for writing before reading it. Rebuilt it from the scratch generator and re-applied every later edit.
